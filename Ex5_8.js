// Função Nomeada
function minhaFuncao() {
    // corpo da função
}

// Função Nomeada com argumento e retorno
function soma(a, b) {
    return a + b;
}

// Função com Construtor
var minhaFuncaoConstrutor = new Function("x", "y", "return x * y");

// Função Literal (atribuída a uma variável)
var minhaFuncaoLiteral = function() {
    // corpo da função
};

// Função de Flecha (Arrow Function)
var somaArrow = (a, b) => a + b;
